import { BLOCK, INLINE } from '@foxy/enums'

import { TBlock, TInline } from '@foxy/types'

export const BLOCK_ARRAY: Array<TBlock> = [BLOCK.TOP, BLOCK.BOTTOM]
export const INLINE_ARRAY: Array<TInline> = [INLINE.START, INLINE.END, INLINE.LEFT, INLINE.RIGHT]