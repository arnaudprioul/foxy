export enum FOCUS_LOCATION {
  NEXT = 'next',
  PREV = 'prev',
  FIRST = 'first',
  LAST = 'last'
}

export enum CLIENT_POSITION {
  Y = 'clientY',
  X = 'clientX'
}

export enum KEYBOARD_VALUES {
  ENTER = 'Enter',
  TAB = 'Tab',
  DELETE = 'Delete',
  ESC = 'Escape',
  SPACE = 'Space',
  UP = 'ArrowUp',
  DOWN = 'ArrowDown',
  LEFT = 'ArrowLeft',
  RIGHT = 'ArrowRight',
  END = 'End',
  HOME = 'Home',
  DEL = 'Delete',
  BACKSPACE = 'Backspace',
  INSERT = 'Insert',
  PAGEUP = 'PageUp',
  PAGEDOWN = 'PageDown',
  SHIFT = 'Shift',
  EMPTY = ' '
}