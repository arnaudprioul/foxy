import type { TAlign } from '@foxui/types'

export interface IAlignProps {
    align?: TAlign,
    alignSm?: TAlign,
    alignMd?: TAlign,
    alignLg?: TAlign,
    alignXl?: TAlign,
    alignXxl?: TAlign
}
