import type { TColor } from "@origam/types"

export interface IColorProps {
    color?: TColor
    bgColor?: TColor
    activeColor?: TColor
    activeBgColor?: TColor
    hoverColor?: TColor
    hoverBgColor?: TColor
}
