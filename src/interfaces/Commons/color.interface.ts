import type { TColor } from "@foxui/types"

export interface IColorProps {
    color?: TColor
    bgColor?: TColor
    activeColor?: TColor
    activeBgColor?: TColor
    hoverColor?: TColor
    hoverBgColor?: TColor
}
