import type { TColor } from "@foxy/types"

export interface IColorProps {
    color?: TColor
    bgColor?: TColor
    activeColor?: TColor
    activeBgColor?: TColor
    hoverColor?: TColor
    hoverBgColor?: TColor
}
