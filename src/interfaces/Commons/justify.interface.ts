import type { TJustify } from '@foxy/types'

export interface IJustifyProps {
    justify?: TJustify,
    justifySm?: TJustify,
    justifyMd?: TJustify,
    justifyLg?: TJustify,
    justifyXl?: TJustify,
    justifyXxl?: TJustify
}
