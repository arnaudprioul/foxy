import type { TPosition } from '@foxy/types'

export interface IPositionProps {
    position?: TPosition

    top?: number | string
    bottom?: number | string
    left?: number | string
    right?: number | string
}
