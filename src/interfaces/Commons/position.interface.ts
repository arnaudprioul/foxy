import type { TPosition } from '@foxui/types'

export interface IPositionProps {
    position?: TPosition

    top?: number | string
    bottom?: number | string
    left?: number | string
    right?: number | string
}
