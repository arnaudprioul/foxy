import type { TPosition } from '@origam/types'

export interface IPositionProps {
    position?: TPosition

    top?: number | string
    bottom?: number | string
    left?: number | string
    right?: number | string
}
