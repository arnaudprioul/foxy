import {
    IBorderProps,
    IColorProps,
    ICommonsComponentProps,
    IElevationProps,
    IMarginProps,
    IPaddingProps
} from "@foxy/interfaces"

export interface IPaginationProps extends ICommonsComponentProps, IColorProps, IBorderProps, IPaddingProps, IMarginProps, IElevationProps {

}
